{
  "carDetails": {
    "carCondition": "newCar | preOwnedCar",
    "make": "", // this should be dropdown in the ui -- stored in backend and retrienved with caching , this is going to be common for all users, ui will also load this and cach this on first load
    "model": "", // this should be dropdown in the ui -- stored in backend and retrienved with caching, this is going to be common for all users, ui will also load this and cach this on first load
    "yearOfManufacturing": "date",
    "plateNumber": "string",
    "mileage": "number",
    "carType": "enum", // this should be dropdown in the ui -- stored in backend and retrienved with caching, this is going to be common for all users, ui will also load this and cach this on first load
    "color": "string",
    "technicalSpecification": {
      "engineNumber": "string",
      "chassisNumber": "string",
      "fuelType": "enum", // this should be dropdown in the ui -- stored in backend and retrienved with caching, this is going to be common for all users, ui will also load this and cach this on first load
      "transmission": "enum", // this should be dropdown in the ui -- stored in backend and retrienved with caching, this is going to be common for all users, ui will also load this and cach this on first load
      "engineCapacity": "number",
      "horsePower": "number"
    },
    "RegistrationAndOwnership": {
      "ownerName": "string",
      "registrationDate": "date",
      "numberOfTransfers": "number",
      "contactNumber": "number",
      "emailAddress": "string",
      "address": "string",
      "logCard": {
        "fileUrl": "string",
        "fileName": "string",
        "mimeType": "string",
        "size": "number"
      }
    },
    "PriceAndCoeDetails": {
      "askingPrice": "number",
      "coeRenewal": "date",
      "coeAmount": "string",
      "downPayment": "number",
      "minimumPARF": "number",
      "depreciation": "number",
      "roadTax": "number",
      "omv": "number"
    },
    "images": {
      "imageWithNumberPlate": "string",
      "carImages": []
    }
  },
  "sellerDetails": {
    "sellerType": "individual | company",
    "ownershipDetails": {
      "registeredOwner": "string",
      "contactNumber": "string",
      "email": "string",
      "address": "string",
      "isSeller": "boolean"
    },
    "sellerDetails": {
      "name": "string",
      "contactNumber": "string",
      "whatsAppNumber": "string",
      "companyAddress": "string",
      "email": "string",
      "letterOfAuthorization": {
        "fileUrl": "string",
        "fileName": "string",
        "mimeType": "string",
        "size": "number"
      }
    }
  },
  "otherDetails":{
    // this should be a key value pair , so that i can extend in future
    "description":"string",
    "carFeatures":"string",
    "accessories":"string"
  }
}